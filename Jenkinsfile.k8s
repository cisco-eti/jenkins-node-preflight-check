podTemplate() {

    node('jenkins-node') {
      stage('simple echo') {
        sh 'echo hello world'
      }
      stage('run build'){
        container('sre-pipeline-docker'){
          sh 'echo test'
          checkout(scm)
        }
      }
    }
}