podTemplate(yaml: '''
    apiVersion: v1
    kind: Pod
''') {

    node('jenkins-node') {
      stage('simple echo') {
        sh 'echo hello world'
      }
      stage('run build'){
        container('sre-pipeline-docker'){
          sh 'echo checkout'
          checkout(scm)
          sh 'll'
        }
      }
    }
}